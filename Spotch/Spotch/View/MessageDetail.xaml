<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             xmlns:local="clr-namespace:Spotch"
             x:Class="Spotch.View.MessageDetail">


  <ContentPage.Content>
    <StackLayout x:Name="messageView">
      
      <Label Text="{Binding time}"
             TextColor="#888"
             FontSize="10"/>
      <Label Text="{Binding message}"
             TextColor="#444"
             FontSize="24"
             HeightRequest="80"/>
      <Label Text="{Binding position.Latitude}"
             TextColor="#888"
             FontSize="10"/>
      <Label Text="{Binding position.Longitude}"
             TextColor="#888"
             FontSize="10"/>
      <Label Text="{Binding postId}"
             TextColor="#888"
             FontSize="10"/>
      <Label Text="{Binding userId}"
             TextColor="#888"
             FontSize="10"/>
      <Label Text="{Binding createAt}"
             TextColor="#888"
             FontSize="10"/>
      
      <maps:Map x:Name="MyMap"
                IsShowingUser="true"
                VerticalOptions="FillAndExpand">
      </maps:Map>
      
    </StackLayout>
  </ContentPage.Content>
</ContentPage>